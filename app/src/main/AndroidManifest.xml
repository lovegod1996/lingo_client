<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lovegod.newbuy">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!--蓝牙权限-->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!--弹窗-->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <uses-permission android:name="android.permission.VIBRATE"/>  <!-- 震动权限 -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera"/>        <!-- 使用照相机权限 -->
    <uses-feature android:name="android.hardware.camera.autofocus"/>   <!-- 自动聚焦权限 -->

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />


    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".view.ShopActivity"></activity>
        <activity android:name=".view.goods.GoodActivity"></activity>
        <activity android:name=".view.goods.CompareActivity"></activity>
        <activity android:name=".view.welcome.WelcomeGuideActivity"></activity>
        <activity android:name=".view.Shop2Activity" />
        <activity android:name=".view.carts.CartActivity"></activity>
        <activity android:name=".view.sorts.SortActivity"></activity>
        <activity android:name=".view.sorts.CategoryGoodsActivity"></activity>
        <activity
            android:name="com.google.zxing.activity.CaptureActivity"
            android:theme="@style/MyAppTheme"/>

        <service android:name=".service.BluetoothService"></service>

        <receiver android:name=".receiver.BlutoothReceiver"></receiver>
        <receiver android:name=".receiver.NotificationReceiver"></receiver>
    </application>

</manifest>